# 🚀 RAG ChromaDB Demo

Демонстрационный проект для обучения работе с **RAG (Retrieval-Augmented Generation)** и векторными базами данных на примере **ChromaDB**.

---

## 📚 Что такое RAG?

**RAG (Retrieval-Augmented Generation)** — это подход в обработке естественного языка, который комбинирует:

1. **Retrieval (Поиск)** — поиск релевантной информации из базы знаний
2. **Augmented (Дополнение)** — использование найденной информации как контекста
3. **Generation (Генерация)** — генерация ответа на основе контекста

### Зачем нужен RAG?

- ✅ Позволяет LLM работать с актуальными данными без переобучения
- ✅ Снижает "галлюцинации" модели
- ✅ Позволяет работать с приватными данными компании
- ✅ Экономит токены, отправляя только релевантную информацию

---

## 🗄️ Зачем используется ChromaDB?

**ChromaDB** — это векторная база данных для хранения и поиска эмбеддингов.

### Основные преимущества:

- 🎯 **Простота** — легко начать работать, не требует сложной настройки
- 💾 **Локальное хранение** — данные хранятся на диске
- 🔍 **Семантический поиск** — ищет по смыслу, а не по точному совпадению
- 🔌 **Интеграция** — легко интегрируется с OpenAI и другими провайдерами
- ⚡ **Производительность** — быстрый поиск даже в больших коллекциях

---

## 📁 Структура проекта

```
rag_chromadb_demo/
├── data/                      # Данные для индексации
│   ├── sample.txt            # Пример текстового файла
│   └── sample.html           # Пример HTML файла
├── loader/                    # Модули для загрузки данных
│   ├── txt_loader.py         # Загрузчик TXT файлов
│   ├── html_loader.py        # Загрузчик HTML файлов
│   └── chunker.py            # Разбиение текста на чанки
├── chroma/                    # Работа с ChromaDB
│   └── chroma_client.py      # Клиент для ChromaDB
├── ingest.py                  # Скрипт загрузки документов
├── search.py                  # Скрипт поиска
├── requirements.txt           # Зависимости проекта
└── README.md                  # Документация (этот файл)
```

---

## 🛠️ Установка и настройка

> ⚠️ **ВАЖНО**: Этот проект использует OpenAI API для создания эмбеддингов по умолчанию. Вам понадобится API ключ!

### 1. Установка зависимостей

```bash
# Создайте виртуальное окружение (рекомендуется)
python -m venv venv

# Активируйте окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка OpenAI API (ОБЯЗАТЕЛЬНО)

**Этот проект использует OpenAI для создания эмбеддингов по умолчанию.**

#### Получите API ключ:
1. Зарегистрируйтесь на https://platform.openai.com/
2. Создайте API ключ: https://platform.openai.com/api-keys
3. Скопируйте ключ

#### Установите ключ:

**Способ 1: Переменная окружения (временно)**
```bash
# Windows PowerShell:
$env:OPENAI_API_KEY="sk-ваш_ключ_здесь"

# Windows CMD:
set OPENAI_API_KEY=sk-ваш_ключ_здесь

# Linux/Mac:
export OPENAI_API_KEY=sk-ваш_ключ_здесь
```

**Способ 2: Файл .env (рекомендуется)**
```bash
# Скопируйте пример
copy .env.example .env

# Отредактируйте .env и вставьте свой ключ
notepad .env
```

В файле `.env`:
```
OPENAI_API_KEY=sk-ваш_настоящий_ключ
```

📖 **Подробная инструкция**: См. [SETUP_OPENAI.md](SETUP_OPENAI.md)

> **Альтернатива**: Если не хотите использовать OpenAI (используются бесплатные встроенные эмбеддинги ChromaDB), добавьте флаг `--no-openai` к командам:
> ```bash
> python ingest.py --no-openai
> python search.py --no-openai --interactive
> ```

---

## 🚀 Быстрый старт

### 🎯 Самый простой способ (ВСЁ В ОДНОМ ФАЙЛЕ):

```bash
# Один файл - весь пайплайн!
python run_all.py
```

Этот файл автоматически:
1. ✅ Проверит OpenAI ключ
2. ✅ Загрузит все документы из `data/`
3. ✅ Создаст эмбеддинги и сохранит в ChromaDB
4. ✅ Откроет интерактивный поиск

Готово! Сразу можете задавать вопросы! 🎉

---

### 📚 Или пошагово:

### Шаг 1: Загрузка документов

```bash
# Загрузить все файлы из папки data/ (использует OpenAI по умолчанию)
python ingest.py

# Или загрузить конкретные файлы
python ingest.py --files data/sample.txt data/sample.html

# Без OpenAI (бесплатные встроенные эмбеддинги ChromaDB)
python ingest.py --no-openai
```

### Шаг 2: Поиск по документам

```bash
# Простой поиск
python search.py "Что такое корпоративная культура?"

# С указанием количества результатов
python search.py "удаленная работа" --n-results 3

# Фильтр по типу документа
python search.py "инструменты" --filter-type txt

# Интерактивный режим
python search.py --interactive
```

---

## 💡 Примеры использования

### Пример 1: Базовая загрузка и поиск

```bash
# 1. Загрузить документы
python ingest.py

# 2. Найти информацию
python search.py "Какие инструменты использует команда?"
```

### Пример 2: Интерактивный режим

```bash
python search.py --interactive

# Теперь можно вводить запросы:
# > Расскажи про корпоративную культуру
# > Какие требования для удаленной работы?
# > exit
```

### Пример 3: Работа с собственными данными

```bash
# 1. Добавьте свои файлы в папку data/
# 2. Загрузите их:
python ingest.py --files data/my_document.txt

# 3. Выполните поиск
python search.py "мой вопрос"
```

---

## 📖 Подробное описание модулей

### 1. `loader/txt_loader.py`

Загрузчик текстовых файлов. Выполняет:
- Чтение файла с правильной кодировкой
- Удаление лишних пробелов
- Нормализация текста

### 2. `loader/html_loader.py`

Загрузчик HTML файлов. Использует BeautifulSoup для:
- Парсинга HTML
- Извлечения чистого текста
- Удаления скриптов и стилей
- Извлечения метаданных

### 3. `loader/chunker.py`

Разбиение текста на чанки. Два режима:
- **Простой** — фиксированный размер с перекрытием
- **Умный** — разбиение по естественным границам (абзацы, предложения)

**Параметры по умолчанию:**
- Размер чанка: 500 символов
- Перекрытие: 100 символов

### 4. `chroma/chroma_client.py`

Клиент для работы с ChromaDB. Функции:
- Создание и управление коллекциями
- Добавление документов с эмбеддингами
- Семантический поиск
- Работа с метаданными

### 5. `ingest.py`

Основной скрипт загрузки:
- Читает файлы из `data/`
- Разбивает на чанки
- Создает эмбеддинги
- Сохраняет в ChromaDB

### 6. `search.py`

Скрипт поиска:
- Принимает текстовый запрос
- Ищет релевантные чанки
- Отображает результаты с метаданными

---

## 🔧 Дополнительные параметры

### ingest.py

```bash
python ingest.py --help

Опции:
  --files FILE [FILE ...]    Список файлов для загрузки
  --chunk-size SIZE          Размер чанка (по умолчанию: 500)
  --overlap SIZE             Перекрытие (по умолчанию: 100)
  --no-openai                НЕ использовать OpenAI (по умолчанию OpenAI используется)
  --openai-key KEY           API ключ OpenAI (или OPENAI_API_KEY в .env)
  --collection NAME          Имя коллекции (по умолчанию: documents)
```

### search.py

```bash
python search.py --help

Опции:
  --interactive, -i          Интерактивный режим
  --n-results N, -n N        Количество результатов (по умолчанию: 5)
  --no-openai                НЕ использовать OpenAI (по умолчанию OpenAI используется)
  --openai-key KEY           API ключ OpenAI (или OPENAI_API_KEY в .env)
  --collection NAME          Имя коллекции
  --filter-type {txt,html}   Фильтр по типу документа
```

---

## 📝 Как добавить новые документы

### Способ 1: Добавить файлы в data/

```bash
# 1. Положите файлы в папку data/
cp /path/to/your/file.txt data/

# 2. Запустите индексацию
python ingest.py
```

### Способ 2: Указать файлы явно

```bash
python ingest.py --files /path/to/file1.txt /path/to/file2.html
```

### Поддерживаемые форматы

- ✅ `.txt` — текстовые файлы
- ✅ `.html`, `.htm` — HTML файлы

---

## 🎓 Что можно улучшить

Этот проект создан для обучения. Возможные улучшения:

1. **Поддержка других форматов**
   - PDF (с помощью PyPDF2 или pdfplumber)
   - DOCX (с помощью python-docx)
   - Markdown

2. **Улучшение чанкинга**
   - Учет семантических границ
   - Рекурсивное разбиение
   - Сохранение структуры документа

3. **Дополнительные метаданные**
   - Дата создания документа
   - Автор
   - Теги и категории

4. **Пользовательский интерфейс**
   - Веб-интерфейс (Streamlit, Gradio)
   - REST API (FastAPI)

5. **Интеграция с LLM**
   - Генерация ответов с помощью GPT
   - Суммаризация результатов
   - Цитирование источников

---

## ❓ FAQ

**Q: Нужен ли мне OpenAI API ключ?**  
A: Да, проект использует OpenAI эмбеддинги по умолчанию для лучшего качества поиска. Если не хотите использовать OpenAI, добавьте флаг `--no-openai`.

**Q: Где хранятся данные?**  
A: Локально в папке `./chroma_db/`

**Q: Можно ли использовать другие модели эмбеддингов?**  
A: Да! Можете модифицировать `chroma_client.py` для использования Hugging Face, Cohere и других.

**Q: Как очистить базу данных?**  
A: Удалите папку `./chroma_db/` или используйте метод `delete_collection()` в коде.

---

## 📚 Полезные ресурсы

- [ChromaDB Documentation](https://docs.trychroma.com/)
- [OpenAI Embeddings Guide](https://platform.openai.com/docs/guides/embeddings)
- [RAG Overview](https://www.pinecone.io/learn/retrieval-augmented-generation/)

---

## 👨‍💻 Автор

Этот проект создан как учебный пример для демонстрации работы с RAG и векторными базами данных.

---

## 📄 Лицензия

Свободно используйте для обучения и экспериментов!

---

**Приятного обучения! 🚀**

